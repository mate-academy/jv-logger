package mate.academy.service;

import java.math.BigDecimal;
import java.util.List;
import mate.academy.model.Order;
import mate.academy.model.Product;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class OrderServiceImpl implements OrderService {
    private static final Logger logger = LoggerFactory.getLogger(OrderServiceImpl.class);

    @Override
    public Order completeOrder(Long userId) {
        // Log that the method completeOrder was called
        logger.debug("Method completeOrder was called. Params: userId={}", userId);

        List<Product> products = getAllProductsFromShoppingCart(userId);
        Order order = new Order(products, userId);
        // NOTE: In production-ready code, this order identifier should be generated by DB
        // For test purposes, we simplify this and return dummy data
        order.setOrderId(1L);

        // Log successful order completion
        logger.info("Order successfully completed for user with ID: {}", userId);
        return order;
    }

    private List<Product> getAllProductsFromShoppingCart(Long userId) {
        // NOTE: In production-ready code, this method should fetch data from DB
        // For test purposes, we simplify this method and return dummy data
        Product iphone = new Product("iPhone X", BigDecimal.valueOf(1199));
        Product macBook = new Product("MacBook Air 2020", BigDecimal.valueOf(1399));
        Product xiaomi = new Product("Xiaomi 12", BigDecimal.valueOf(499));
        List<Product> products = List.of(iphone, macBook, xiaomi);

        // Log successful fetched data from DB
        logger.debug(
                "Fetched products from shopping cart for user with ID {}: {}",
                userId,
                products);
        return products;
    }
}
